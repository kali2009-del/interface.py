interface.py

interface.py provides a simple command-line interface (CLI) that allows users to easily interact with and execute the network scanning tools included in this project. It acts as a central controller to organize tool execution and improve overall usability.
